services:
  grid-bot:
    build: .
    container_name: t-invest-grid-bot
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - grid-bot-data:/app/data
    restart: unless-stopped

volumes:
  grid-bot-data:
